{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "uv pip install -e '.[saas]'",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}
