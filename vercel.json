{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "uv pip install --system -e '.[saas]'",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}
